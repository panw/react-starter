import React from "react"
import ReactDOM from "react-dom"
import TestUtils from "react-addons-test-utils"

describe("Test Setup Confirmation", () => {
	it('true should equal true', () => {
		expect(true).toEqual(true);
	});
});
